# Step 06 — Setup Prettier
**Course Progress:** `06 / 08`

📅 **Published**: 05 April 2026  
🔄 **Last Updated**: 07 April 2026  
✍️ **Maintained By**: Kaustubh Gawade

---

In the previous step, we configured TypeScript for our project. Our code now has type safety in place.

But type safety is only one part of writing good code. The other part is consistency — making sure every file in your project looks and feels the same, regardless of who wrote it or which editor they used.
 

## What You Will Learn in This Step
In this step we will learn how to:

- understand what Prettier is and what problem it solves
- understand why formatting matters in a production project
- create and configure the `.prettierrc` file
- understand what each field in the config does and why it is there
- set up `.prettierignore` to tell Prettier which folders to skip
- verify that Prettier is working correctly
- understand the Prettier VS Code extension and how it differs from the Prettier package

By the end of this step, your project will have a consistent, enforced formatting setup that every developer on the team works with automatically.

---

## What is Prettier
Prettier is an **opinionated code formatter.**

When you write code, you make dozens of small formatting decisions without even thinking about it — should there be a semicolon at the end of this line? Single quotes or double quotes? How long should this line be before it wraps? Two spaces or four? 

These decisions are small individually. But across a team of developers, across hundreds of files, across months of development — they accumulate. Every developer has their own preferences. Every editor has its own defaults. The result is a codebase where every file looks slightly different, code reviews get cluttered with formatting comments, and merges create unnecessary conflicts.

Prettier solves this by taking the decision away entirely.

You configure it once. It enforces that configuration everywhere. Every file, every developer, every time.

---

## Why Prettier Over Manual Formatting
You might be thinking — *"I am careful about formatting. I do not need a tool for this."*

Here is the honest answer: it is not about you.

The moment a second developer joins the project, you have two sets of habits. A third developer makes it three. Everyone thinks they are formatting correctly — and they all are, just differently.

Prettier removes the conversation entirely. There are no more formatting debates in code reviews. No more *"can you add a semicolon here"* or *"we use single quotes in this project"* comments. The formatter handles all of it automatically, on every save, for every developer.

In a production project or a microservice that multiple developers will touch, this consistency is not a nice-to-have. It is a requirement.

---

## Setting Up Prettier
Prettier does not have an init command like TypeScript does. There is no yarn prettier --init. You simply create the config file manually.

In the root of your project, create a file called .prettierrc:
```bash
touch .prettierrc
```
Then open it and paste the following config:
```json
{
  "printWidth": 120,
  "tabWidth": 2,
  "useTabs": false,

  "semi": true,
  "singleQuote": true,
  "quoteProps": "as-needed",
  "trailingComma": "all",

  "bracketSpacing": true,
  "arrowParens": "always",

  "endOfLine": "lf",
  "proseWrap": "always",
  "embeddedLanguageFormatting": "auto"
}
```
Save the file.

> If you want to understand what each field in this config does and why we chose the value we did, we have a dedicated deep dive for it. Head over to Prettier Config [Deep Dive →](./prettier-config-explained.md)

## Setting Up .prettierignore
Just like `.gitignore` tells Git which files to ignore, `.prettierignore` tells Prettier which files and folders to skip when formatting.

Create the file in the root of your project:
```bash
touch .prettierignore
```
Then add the following:
```bash
node_modules
dist
coverage
*.log
```
Here is why each one is there:

`node_modules` — installed packages. You never want to format these. They are not your code.
`dist` — compiled output. This is generated by TypeScript. Formatting it is pointless since it gets regenerated on every compile.
`coverage` — test coverage reports. Auto-generated files that should never be touched.
`*.log` — log files. Not source code.


## Verifying the Setup
Let us make sure Prettier is working correctly. Run this command:
```bash
yarn prettier --check .
```
This tells Prettier to check all files in the project against your config without actually changing anything.   

If everything is formatted correctly, you will see:
```bash
All matched files use Prettier formatting!
```
If there are files that do not match the config, Prettier will list them. To automatically fix them, run:
```bash
yarn prettier --write .
```
This formats every file in the project according to your .prettierrc config.

---

## The Prettier VS Code Extension
You may have come across the Prettier - Code formatter extension in the VS Code marketplace. A common question is — if I already have Prettier installed in my project, do I still need the extension? Are they the same thing?

They are not the same thing. They serve different purposes and you need both.

Here is the distinction:   
- **Prettier installed in your project** (devDependencies) is the actual formatter. It is what runs when you execute `yarn prettier --write .` in the terminal. It is what your CI pipeline will use to check formatting. It is the source of truth for your entire team.
- **The VS Code extension** is a bridge between your editor and that installed package. It allows VS Code to trigger Prettier automatically — on every save, on every paste, or on demand — without you having to run a terminal command each time. Without the extension, VS Code has no idea Prettier exists. Without the package, the extension has nothing to run.

Install the extension from the VS Code marketplace:
👉 [Prettier - Code formatter](https://marketplace.visualstudio.com/items?itemName=esbenp.prettier-vscode)  

Once installed, add these settings to your VS Code workspace settings (`.vscode/settings.json`):
```json
{
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.formatOnSave": true
}
```

`editor.defaultFormatter` — tells VS Code to use Prettier as the default formatter for all files
`editor.formatOnSave` — automatically runs Prettier every time you save a file

With this in place, you never have to think about formatting again. Save the file, Prettier handles it.

> **Should you commit** `.vscode/settings.json` to GitHub? Yes — commit it. This ensures every developer who clones the project gets the same VS Code behaviour automatically. No manual setup required on their end.

--- 

## Prettier vs ESLint — Where One Ends and the Other Begins
Since ESLint is the next step, it is worth drawing this boundary clearly before you get there.

Prettier handles how your code looks. ESLint handles how your code works.

Prettier cares about formatting — indentation, quotes, semicolons, line length. ESLint cares about code quality — unused variables, unreachable code, potential bugs, and enforcing best practices.

They are complementary tools, not competing ones. In a production project you need both — Prettier to keep code consistent, ESLint to keep code correct.

---

## Running Prettier as a Script
Instead of remembering the full `yarn prettier --write .` command every time, add a script to your `package.json`:
```json
"scripts": {
  "format": "prettier --write .",
  "format:check": "prettier --check ."
}
```
Now anyone on the team can run:
```bash
yarn format
```
to format the entire project, or:
```bash
yarn format:check
```
to check formatting without making any changes. Clean, simple, and consistent across the team.

#### Taking It Further — Git Hooks and GitHub Actions
Right now Prettier runs when someone manually calls yarn format. But in a real production project, you want formatting to be automatic and enforced — not dependent on a developer remembering to run a command.  
Two things make this possible:
- Git hooks with husky and lint-staged — Prettier runs automatically before every commit. If the code is not formatted, the commit does not go through. We will set this up in a later step.
- GitHub Actions — Prettier runs as part of your CI pipeline on every pull request. If formatting check fails, the PR cannot be merged. This is the last line of defence that ensures nothing slips through regardless of local setup. 

We will get to these steps in futher blogs, hang on till than.

---

Congratulations 🎉  

Your project now has consistent, enforced formatting in place. Every file will look the same regardless of who writes it or which editor they use.

--- 

> Back Step: **[TypeScript Setup](../04-install-dependencies/README.md)**

> Next Step: **[ESLint Setup](../07-eslint-setup/README.md)**